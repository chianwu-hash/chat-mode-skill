interface:
  display_name: "Chat Mode"
  short_description: "Coordinate bounded Claude CLI and Desktop collaboration"
  default_prompt: "Use $chat-mode for supervised Claude CLI review, resumed discussion, isolated implementation, or an explicit Desktop handoff."
