interface:
  display_name: "Chat Mode"
  short_description: "Coordinate prompt-free review and implementation"
  default_prompt: "Use $chat-mode for a prompt-free read-only Claude review, isolated implementation, or an explicit direct main-worktree handoff."
