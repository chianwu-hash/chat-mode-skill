interface:
  display_name: "Chat Mode"
  short_description: "Coordinate bounded Claude review and implementation"
  default_prompt: "Use $chat-mode for a bounded Claude read-only review, isolated implementation, or explicit direct main-worktree handoff."
